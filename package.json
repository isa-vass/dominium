{
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express-session": "^1.19.0"
  },
  "scripts": {
   "start": "sudo apt-get install -y mysql-server -qq && sudo service mysql start && node server/server.js"
  }
}